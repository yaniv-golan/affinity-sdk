name: Version Consistency Check

on:
  pull_request:  # Run on ALL PRs - the check is fast and catches edge cases
  push:
    branches: [main]  # Ensure main is always validated

jobs:
  check-versions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Extract SDK version
        id: sdk
        run: |
          SDK_VERSION=$(grep '^version = ' pyproject.toml | head -1 | cut -d'"' -f2)
          echo "version=$SDK_VERSION" >> "$GITHUB_OUTPUT"

      - name: Check MCP CLI compatibility
        shell: bash
        run: |
          source mcp/COMPATIBILITY
          SDK_VERSION="${{ steps.sdk.outputs.version }}"
          SDK_MINOR=$(echo "$SDK_VERSION" | cut -d. -f1-2)
          MIN_MINOR=$(echo "$CLI_MIN_VERSION" | cut -d. -f1-2)
          # SDK minor version should be >= CLI_MIN_VERSION minor
          if [ "$(printf '%s\n%s' "$MIN_MINOR" "$SDK_MINOR" | sort -V | head -1)" != "$MIN_MINOR" ]; then
            echo "::error::SDK version $SDK_VERSION is below MCP minimum ($CLI_MIN_VERSION)"
            exit 1
          fi

      - name: Check MCP server.meta.json matches VERSION
        run: |
          MCP_VERSION=$(cat mcp/VERSION | tr -d '[:space:]')
          META_VERSION=$(jq -r '.version' mcp/server.d/server.meta.json)
          if [ "$META_VERSION" != "$MCP_VERSION" ]; then
            echo "::error::server.meta.json version ($META_VERSION) != VERSION file ($MCP_VERSION)"
            exit 1
          fi
          echo "MCP server version: $MCP_VERSION"

      - name: Validate plugin.json files
        run: |
          # Just verify valid JSON and version field exists (no cross-file sync required)
          for f in plugins/affinity-sdk/.claude-plugin/plugin.json \
                   plugins/xaffinity-cli/.claude-plugin/plugin.json \
                   mcp/.claude-plugin/plugin.json; do
            VERSION=$(jq -r '.version // empty' "$f")
            if [ -z "$VERSION" ]; then
              echo "::error::Missing version in $f"
              exit 1
            fi
            echo "$f: $VERSION"
          done

      - name: Check mcp-bash lockfile
        run: |
          # Verify lockfile exists
          if [ ! -f mcp/mcp-bash.lock ]; then
            echo "::error::mcp/mcp-bash.lock not found"
            exit 1
          fi

          # Source and validate lockfile
          source mcp/mcp-bash.lock

          # Validate version format
          if ! [[ "$MCPBASH_VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "::error::Invalid MCPBASH_VERSION format: $MCPBASH_VERSION"
            exit 1
          fi
          echo "Framework version: $MCPBASH_VERSION"

          # Validate commit hash format
          if ! [[ "$MCPBASH_COMMIT" =~ ^[0-9a-f]{40}$ ]]; then
            echo "::error::Invalid MCPBASH_COMMIT format: $MCPBASH_COMMIT"
            exit 1
          fi
          echo "Framework commit: $MCPBASH_COMMIT"

          # Verify vendored runtime matches lockfile version
          VENDOR_VERSION=$(cat mcp/.mcp-bash/VERSION 2>/dev/null || echo "")
          if [ -z "$VENDOR_VERSION" ]; then
            echo "::error::Vendored runtime not found at mcp/.mcp-bash/"
            exit 1
          elif [ "$VENDOR_VERSION" != "$MCPBASH_VERSION" ]; then
            echo "::error::Vendored version ($VENDOR_VERSION) does not match lockfile ($MCPBASH_VERSION)"
            exit 1
          else
            echo "Vendored runtime version matches lockfile"
          fi
